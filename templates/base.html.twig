<!DOCTYPE html>
<html lang="fr" dir="ltr" data-theme="black">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{% block title %}Dédale • Bordeaux • Musique expérimentale & arts déviants{% endblock %}</title>
    <meta name="description" content="Chambre d'écho au champ immense des musiques et pratiques expérimentales, en lien avec les différents acteurs et associations soutenant cette scène, dans le but plus vaste d'éduquer les oreilles et autres sens pour inciter à la curiosité.">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    {% block stylesheets %}{% endblock %}
    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body class="min-h-screen grid grid-cols-12 gap-4" {{ stimulus_controller('scroller') }}>
    {% block header %}
        <header role="banner" class="col-span-12 lg:col-span-3 m-6 mb-0 lg:m-16 mr-3">
            <div class="drawer">
                <input id="main-menu" type="checkbox" class="drawer-toggle" />
                <div class="drawer-content z-1">
                    <div class="flex items-start justify-between lg:block lg:mb-12">
                        {% include '_logo.html.twig' %}
                        <label for="main-menu" class="lg:hidden btn bg-black cursor-pointer border-1 border-dashed border-white hover:border-transparent p-2">
                            <span class="font-expanded text-base">
                                menu
                            </span>
                        </label>
                    </div>
                </div>
                <div class="drawer-side z-2">
                    <label for="main-menu" aria-label="close sidebar" class="drawer-overlay"></label>
                    <nav role="navigation" class="bg-base-100 min-h-full p-6">
                        <div class="mb-12">
                            {% include '_logo.html.twig' %}
                        </div>
                        <ul role="list" class="space-y-6">
                            {% include '_menuitems.html.twig' %}
                        </ul>
                    </nav>
                </div>
            </div>
            <nav role="navigation" aria-label="Menu principal" class="hidden lg:block">
                <ul class="flex flex-col space-y-6">
                    {% include '_menuitems.html.twig' %}
                </ul>
            </nav>
        </header>
    {% endblock %}
    {% block body %}
        <main class="col-span-12 md:col-span-8 lg:col-span-6" tabindex="-1">
            <section class="m-4 lg:m-6 mb-0 lg:m-16 lg:ml-4">
                <div id="main-content" class="z-1 relative">
                    <h1 class="font-expanded break-all text-4xl lg:text-6xl lg:pt-17 pb-3">
                        {% block heading %}{% endblock %}
                    </h1>
                    <h2 class="font-mono text-xl pb-12">
                        {% block subheading %}{% endblock %}
                    </h2>
                    {% block content %}{% endblock %}
                </div>
            </section>
        </main>
        <aside class="w-[320px] lg:w-1/3 fixed right-0 opacity-55 md:opacity-95 z-0" {{ stimulus_target('scroller', 'maze') }}>
            {% block maze %}
                <header class="h-screen relative overflow-y-auto">
                    <img src="{{ asset('images/maze-animated.svg') }}" alt="" role="presentation">
                </header>
            {% endblock %}
            {% block aside %}{% endblock %}
        </aside>
    {% endblock %}
</body>
</html>
